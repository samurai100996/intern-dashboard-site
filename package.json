{
  "name": "intern-dashboard-site",
  "version": "1.0.0",
  "description": "Full-stack intern dashboard with React frontend and Express backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["intern", "dashboard", "react", "express", "fullstack"],
  "author": "Saurav Shelke",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 